# Copy to .env.local (or .env) and fill in. All .env* files except this one are gitignored.
#
# Format: one KEY=value per line. No spaces around =. Comments start with #.
# Optional: wrap values with spaces in double quotes.
#
# Golf Genius API (required for API calls)
GOLF_GENIUS_API_KEY=your_api_key_here

# Optional: use staging API (ggstest.com) instead of production (www.golfgenius.com)
# GOLF_GENIUS_ENV=staging

# Optional: custom base URL (overrides GOLF_GENIUS_ENV when set)
# GOLF_GENIUS_BASE_URL=https://ggstest.com
